[package]
name = "tos-tests-fuzz"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Deterministic mutation fuzz target for the capsule parser (total/bounded property)."
publish = false

[dependencies]
tos-capsule = { path = "../../crates/capsule", default-features = false }
tos-hash = { path = "../../crates/tos-hash" }