# SPDX-License-Identifier: GPL-3.0-or-later
[package]
name = "tos-engine"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Bounded Bootstrap reference interpreter for verified tos-ir/v1."
publish = false

[lib]
name = "tos_engine"

[dependencies]
tos-ir = { path = "../tos-ir" }
tos-verifier = { path = "../tos-verifier" }
