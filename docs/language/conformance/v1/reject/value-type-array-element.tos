// SPDX-License-Identifier: GPL-3.0-or-later
module conformance.value_type_array_element version 1.0 profile bootstrap;

resource [fuel: 1000, stack: 64KiB, allocation: 4KiB, tasks: 1, workers: 1, sync: 0, shared: 0B, cleanup: 16, recursion: 8, imports: 0]

// The annotation declares an array of `i64` and the literal builds one of
// `bool`. The aggregate's type comes from its components and its declared type,
// and here they disagree.
pub fn main() -> unit {
    let flags: array<i64, 2> = [true, false];
}
