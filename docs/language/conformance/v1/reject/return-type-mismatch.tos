// SPDX-License-Identifier: GPL-3.0-or-later
module conformance.return_type_mismatch version 1.0 profile bootstrap;

resource [fuel: 1000, stack: 64KiB, allocation: 4KiB, tasks: 1, workers: 1, sync: 0, shared: 0B, cleanup: 16, recursion: 8, imports: 0]

// The declared result is `i32`; a `bool` is not it, and an unsuffixed integer
// literal would have been contextually typed.
pub fn main(ready: bool) -> i32 {
    return ready;
}
