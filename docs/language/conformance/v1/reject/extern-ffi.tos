// SPDX-License-Identifier: GPL-3.0-or-later
module conformance.extern_ffi version 1.0 profile full;

resource [fuel: 1000, stack: 64KiB, allocation: 4KiB, tasks: 1, workers: 1, sync: 0, shared: 0B, cleanup: 16, recursion: 8, imports: 0]

// V1 accepts no FFI interface schema, so every extern item is rejected.
extern fn host_write(value: i32) -> unit;

pub fn main() -> unit { }
