// SPDX-License-Identifier: GPL-3.0-or-later
module conformance.nonconstructible_shared_cast version 1.0 profile bootstrap;

resource [fuel: 1000, stack: 64KiB, allocation: 4KiB, tasks: 1, workers: 1, sync: 0, shared: 0B, cleanup: 16, recursion: 8, imports: 0]

// A `Shared<T>` comes only from the typed share contract.
pub fn main() -> unit {
    let forged = 1u64 as Shared<i32>;
}
