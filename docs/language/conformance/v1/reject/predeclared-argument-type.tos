// SPDX-License-Identifier: GPL-3.0-or-later
module conformance.predeclared_argument_type version 1.0 profile bootstrap;

resource [fuel: 1000, stack: 64KiB, allocation: 4KiB, tasks: 1, workers: 1, sync: 0, shared: 0B, cleanup: 16, recursion: 8, imports: 0]

// docs/40 §3: a checked conversion accepts any fixed-width integer or `size`.
// A `bool` is neither, and no more specific code describes it.
pub fn main() -> i64 {
    to_u8(true);
    return 0i64;
}
